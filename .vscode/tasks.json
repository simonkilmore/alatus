{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
			"label": "gulp-build",
            "type": "gulp",
            "task": "build",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
			"label": "gulp-watch",
			"command": "gulp",
			"args": ["watch"],
            "type": "shell",
			"auto": true,
            "problemMatcher": []
        }
    ]
}